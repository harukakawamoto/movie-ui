interface Movie {
  title: string;
  overview: string;
  runtime: number;
  release_date: string;
  thumbnail: string;
}

interface SearchMovie {
  id: number;
  title: string;
  release_date: string;
  thumbnail: string;
}
